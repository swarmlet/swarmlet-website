version: '3.7'

services:
  website:
    image: ${SWARMLET_REGISTRY}/swarmlet-website-staging
    build: .
    networks:
      - traefik-public
    deploy:
      mode: replicated
      replicas: 1
      labels:
        - traefik.enable=true
        - traefik.tags=traefik-public
        - traefik.docker.network=traefik-public
        - traefik.swarmlet-website-staging.enable=true
        - traefik.swarmlet-website-staging.port=80
        - traefik.swarmlet-website-staging.frontend.rule=Host:${DOMAIN}
        - traefik.swarmlet-website-staging.frontend.entryPoints=http,https
        - traefik.swarmlet-website-staging.frontend.passHostHeader=true
        - traefik.swarmlet-website-staging.frontend.headers.SSLRedirect=true
        - traefik.swarmlet-website-staging.frontend.headers.SSLHost=${DOMAIN}
        - traefik.swarmlet-website-staging.frontend.headers.STSIncludeSubdomains=true
        - traefik.swarmlet-website-staging.frontend.headers.STSPreload=true

networks:
  traefik-public:
    external: true
